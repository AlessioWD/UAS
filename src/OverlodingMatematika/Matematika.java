/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
package OverlodingMatematika;

/**
 *
 * @author Toshiba
 */
public class Matematika {

    void pertambahan(double a, double b, double c) {
        double hasil = a + b + c;
        System.out.println("\nhasil pertambahan (pecahan) : " + hasil);
    }

    void pertambahan(double a, double b) {
        double hasil = a + b;
        System.out.println("hasil pertambahan (pecahan) : " + hasil);
    }

    void pertambahan(int a, int b, int c) {
        int hasil = a + b + c;
        System.out.println("hasil pertambahan (integer) : " + hasil);
    }

    void pertambahan(int a, int b) {
        int hasil = a + b;
        System.out.println("hasil pertambahan (integer) : " + hasil);
    }

    void pengurangan(double a, double b, double c) {
        double hasil = a - b - c;
        System.out.println("\nhasil pengurangan (pecahan) : " + hasil);
    }

    void pengurangan(double a, double b) {
        double hasil = a - b;
        System.out.println("hasil pengurangan (pecahan) : " + hasil);
    }

    void pengurangan(int a, int b, int c) {
        int hasil = a - b - c;
        System.out.println("hasil pengurangan (integer) : " + hasil);
    }

    void pengurangan(int a, int b) {
        int hasil = a - b;
        System.out.println("hasil pengurangan (integer) : " + hasil);
    }

    void perkalian(double a, double b, double c) {
        double hasil = a * b * c;
        System.out.println("\nhasil perkalian (pecahan) : " + hasil);
    }

    void perkalian(double a, double b) {
        double hasil = a * b;
        System.out.println("hasil perkalian (pecahan) : " + hasil);
    }

    void perkalian(int a, int b, int c) {
        int hasil = a * b * c;
        System.out.println("hasil perkalian (integer) : " + hasil);
    }

    void perkalian(int a, int b) {
        int hasil = a * b;
        System.out.println("hasil perkalian (integer) : " + hasil);
    }

    void pembagian(double a, double b, double c) {
        double hasil = a / b / c;
        System.out.println("\nhasil pembagian (pecahan) : " + hasil);
    }

    void pembagian(double a, double b) {
        double hasil = a / b;
        System.out.println("hasil pembagian (pecahan) : " + hasil);
    }

    void pembagian(int a, int b, int c) {
        int hasil = a / b / c;
        System.out.println("hasil pembagian (integer) : " + hasil);
    }

    void pembagian(int a, int b) {
        int hasil = a / b;
        System.out.println("hasil pembagian (integer) : " + hasil);
    }

    void modulus(double a, double b, double c) {
        double hasil = a % b % c;
        System.out.println("\nhasil modulus (pecahan) : " + hasil);
    }

    void modulus(double a, double b) {
        double hasil = a % b;
        System.out.println("hasil modulus (pecahan) : " + hasil);
    }

    void modulus(int a, int b, int c) {
        int hasil = a % b % c;
        System.out.println("hasil modulus (integer) : " + hasil);
    }

    void modulus(int a, int b) {
        int hasil = a % b;
        System.out.println("hasil modulus (integer) : " + hasil);
    }
}
